# Pitch 视频脚本（3:00）

这是提交给 Colosseum 的 **pitch video** 脚本——单一最重要的交付物。评委会先看前 30 秒决定是否深入研究。

**工具**：Loom 或 Descript。1080p。创始人脸在右侧，屏幕 demo 在左侧。

**语调**：自信、技术、冷幽默。不要推销腔。

---

## 节拍表

| 时间 | 节拍 | 视觉 |
|---|---|---|
| 0:00 – 0:30 | 钩子（段子） | 终端 + tool trace + 答案 |
| 0:30 – 1:00 | 问题 | 白板风格 |
| 1:00 – 1:45 | 产品（实时 Curio） | 屏幕录制、配音 |
| 1:45 – 2:15 | 为什么 Solana | 动画 slide |
| 2:15 – 2:45 | 商业 + Traction | 图表 + 数字 |
| 2:45 – 3:00 | Ask + 收尾 | 创始人脸全屏 |

---

## 中文版台词（如果选中文录制）

### 0:00 – 0:30 — 钩子

> *（视觉：终端光标闪烁）*
>
> *（一字一字打）*
>
> ```
> $ npm install @tollgate/middleware
> $ # 一行代码
> $ git push
> ```
>
> *（切换到 Claude tool-use trace，快剪）*
>
> ```
> Agent: 正在查询付费数据源...
>   ✓ NewsAPI       paid $0.002 USDC
>   ✓ ArXiv         paid $0.003 USDC
>   ✓ Wikipedia     paid $0.0005 USDC
>   ✓ Solana Docs   paid $0.0005 USDC
> ```
>
> *（切换到最终答案，带引用 + 成本明细 + 链上 Explorer 链接）*
>
> *（切到创始人脸，平静地看镜头）*
>
> "我们刚刚把 HTTP 402 跑通了。在 Solana 上。给 AI 智能体用。
> 双向各 30 行代码。
> 这是下一个十亿美金的 API 经济。"
>
> *（Logo: Tollgate）*

### 0:30 – 1:00 — 问题

> *（slide：白板图——左边是"AI Agent"，右边是"API"，中间是断开的箭头）*
>
> "Stripe 是为人类设计的。AI 智能体没有身份证、没有信用卡、没有银行账户。"
>
> *（动画：每一项被划掉）*
>
> "今天，每个智能体都共用主人的 API key。这破坏了所有有趣的智能体设计。
>
> 智能体没法动态发现*新*的付费 API。
> 平台没法按智能体计费。
> $0.001 的微支付 Visa 直接拒绝。"
>
> *（视觉：$0.001 + "Stripe 拒绝：低于最低金额" 印章）*
>
> "智能体网络需要不一样的支付底层。"

### 1:00 – 1:45 — 实时 Curio Demo

> *（浏览器打开 Curio）*
>
> "这是 Curio。问任何问题。看着你的 AI 实时给数据源付钱。"
>
> *（输入提示）*
>
> > "What's the latest research on Solana's Firedancer upgrade?"
>
> *（按回车；tool 列表动画显示每个调用落位）*
>
> "Claude 选了 4 个数据源。每个都是付费 API。看那些圆点。"
>
> *（指着 UI 的滚动）*
>
> "$0.002 给 NewsAPI。$0.003 给 ArXiv。$0.0005 给 Wikipedia。$0.0005 给 Solana Docs。
>
> 每一笔都是 Solana 上的稳定币转账——亚秒级、亚分手续费。
>
> *（答案流式生成）*
>
> "现在 Claude 合成带引用的答案。这是收据——
>
> *（滚动到成本明细）*
>
> "总共 $0.0085 USDC。点任意一行，直接进 Solana Explorer。可审计。链上。Stripe 完全做不到这个。"

### 1:45 – 2:15 — 为什么是 Solana

> *（动画 slide："Why this only works on Solana"，5 行）*
>
> "我们需要 5 件事同时成立："
>
> *（每行带勾出现）*
>
> 1. 亚分手续费——这样调用费不会比调用本身贵
> 2. 亚秒级最终性——一个智能体打 100 次调用不能堵死
> 3. 无 KYC——智能体没有身份
> 4. 原生 USDC——预算可预测
> 5. 可编程分账——多人维护的 API 需要
>
> "以太坊 L2 给你一两条。Lightning 给你亚分但没稳定币。Stripe 一条都不给。
>
> Solana 是当下唯一同时满足 5 条的链。"

### 2:15 – 2:45 — 商业 + Traction

> *（视觉：折线图，注意：禁止伪造 traction，用真实数据）*
>
> "Tollgate 是开源 MIT。两个 npm 包——middleware 和 agent，加一个结算服务。我们用这次黑客松启动这个协议。
>
> 平台在每次结算上抽 5-10 个百分点。智能体 API 经济到 2027 年预计 500 亿美金。
>
> Cypherpunk 的 MCPay 通过服务 MCP 获奖。Latinum 通过服务支付中间件获奖。Tollgate 是这两者下面的通用层——任何付费 HTTP API，任何 agent SDK。
>
> 如果我们对了，开放网络上每个付费端点最终都会有一个 402 层。"

### 2:45 – 3:00 — Ask + 收尾

> *（创始人全屏）*
>
> "我是 Moc01。我用 11 天做了它，因为我需要它。我计划黑客松后 90 天内把它上线为生产基础设施。加速器是我达成这个目标的方式。
>
> Tollgate。AI 智能体的自动售货机。
> 代码在 GitHub。Curio 在链接里。谢谢。"
>
> *（结束卡：tollgate.dev · github.com/Moc01/tollgate）*

---

## 录制 checklist

- [ ] 测试麦克风音量；说话比自然语速慢约 10%
- [ ] 录制前 5 分钟预热 Vercel 部署（冷启动会毁 demo）
- [ ] 用 Loom 的"Cam + Screen"模式 1080p
- [ ] 严格控制在 3:00 内（Colosseum 自动拒绝超长）
- [ ] 反复重录钩子段；它是 50% 的评分
- [ ] 上传为**未列出**的 YouTube 和**公开** Loom（评委两个都看）
- [ ] 加字幕（Descript 自动生成）
- [ ] 最终用秒表 review
